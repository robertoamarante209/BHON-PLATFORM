# Handoff BHON — 09/09/2026

Copie e cole o prompt abaixo em outro chat para continuar o desenvolvimento sem perder contexto.

## Prompt

Você está assumindo o desenvolvimento full-stack da **BHON — A clínica no controle**. Trabalhe como desenvolvedor sênior e product designer, preserve o que já está funcionando e seja rigoroso com segurança, acessibilidade, responsividade, estados honestos e dados reais. Não invente números ou registros clínicos.

### Repositório e produção

- GitHub: https://github.com/robertoamarante209/BHON-PLATFORM
- Branch: `main`
- Último commit deste handoff: consulte o HEAD remoto de `main` (o commit funcional anterior é `816dacb3861609265ae5a9a0526e257df9f884be`).
- Produção: https://bhon-platform.vercel.app
- Supabase: projeto `pngxrwsvnqghloebbbba` (`bhon - saas`), ativo e saudável.
- Comece clonando ou atualizando a partir do `main` remoto. Não reaproveite históricos locais divergentes de sessões anteriores.

### O que já foi feito

1. **Marca e experiência de entrada**
   - Logo oficial salva em `frontend/public/logo-official.jpg`.
   - Antes do login existe uma animação suave de apresentação da BHON em `BrandIntro.tsx`.
   - A apresentação aparece uma vez por sessão do navegador, dura aproximadamente 2,2 segundos, permite pular e respeita `prefers-reduced-motion`.
   - Texto: “Clareza para cuidar. Controle para crescer.”

2. **Sessão e logout**
   - `POST /auth/logout` foi corrigido para ser idempotente.
   - Revogação usa `updateMany` apenas em sessões ainda não revogadas.
   - Há teste de regressão para logout repetido.

3. **Configurações, salas e indicadores reais**
   - Endpoints implementados e registrados:
     - `GET /api/settings`
     - `POST /api/rooms`
     - `PATCH /api/rooms/:id`
     - `GET /api/indicators?period=TODAY|WEEK|MONTH`
   - Escopo por clínica, RBAC, auditoria e validação de nomes duplicados.
   - `SettingsPage` consome API real e permite criar/editar salas.
   - `IndicatorsPage` usa métricas reais e tem estados de carregamento, erro e vazio.
   - Ocupação de salas permanece `null` até os horários de funcionamento serem configurados, sem exibir dados inventados.
   - Foi criada apenas a sala real inicial `Consultório 01`, de forma idempotente e com auditoria.

4. **Agenda e fuso horário**
   - Intervalos diários no backend respeitam `America/Sao_Paulo`.
   - O frontend converte o horário local da clínica para ISO sem depender do fuso do navegador.
   - Utilitário em `frontend/src/lib/datetime.ts`, com testes.
   - Fluxo integrado da agenda cobre paciente → profissional → sala → procedimento → horário.

5. **Frontend, responsividade e acessibilidade**
   - Navegação inferior mobile, menu mobile completo, skip link, Escape, foco visível, alvos de toque de 44 px e redução de movimento.
   - Páginas carregadas por rota com lazy loading/Suspense; bundle inicial reduzido de cerca de 472 KB para aproximadamente 240 KB.
   - Drawer com semântica de diálogo, foco, bloqueio de rolagem e Escape.
   - Times usam API real; pacientes têm botão explícito e acessível para abrir detalhes.
   - Dados demonstrativos operacionais ficaram restritos às páginas de plataforma; a clínica não carrega números falsos em indicadores/configurações/time.

6. **Banco, segurança e qualidade**
   - Sete migrations verificadas, sem divergências de colunas ou enums.
   - Supabase Security Advisors: nenhum alerta (`lints: []`).
   - Auditorias de dependências de produção do backend e frontend: zero vulnerabilidades conhecidas de severidade alta.
   - Última verificação: 36 testes do backend + 8 do frontend = 44 testes aprovados; builds do backend e frontend aprovados.
   - Produção respondeu `200` em `/health/live` e `/logo-official.jpg`.
   - A introdução com a logo foi conferida visualmente em produção.

### O que precisa terminar

Priorize nesta ordem:

1. **Smoke test autenticado em produção**
   - Validar login, indicadores, configurações/salas, agenda completa, detalhe de paciente e logout duas vezes.
   - Testar larguras 375, 768, 1366 e 1920 px.
   - Para entrar, peça confirmação apenas no instante da ação segura ou deixe o usuário autenticar manualmente. Nunca solicite nem registre senha no chat, código ou logs.

2. **Eliminar pendências antes do lançamento amplo**
   - Remover o e-mail administrativo preenchido por padrão na tela de login; o campo deve iniciar vazio.
   - Fazer auditoria final de acessibilidade e responsividade nas páginas antigas de plataforma, orçamentos, tratamentos e follow-ups, removendo interações exclusivas por clique na linha.
   - Substituir os dados demonstrativos restantes das páginas do proprietário da plataforma por APIs reais ou estados explicitamente identificados como não configurados.

3. **Completar configurações clínicas reais**
   - Implementar horários de funcionamento; então calcular ocupação de salas.
   - Implementar CRUD real de procedimentos, protocolos clínicos, convites/usuários/permissões e configurações de integrações.
   - Tornar o fuso horário configurável por clínica no banco; hoje há configuração global por `BHON_TIME_ZONE` e `VITE_BHON_TIME_ZONE`, com padrão `America/Sao_Paulo`.

4. **Integrações após a fase de testes**
   - Definir e integrar WhatsApp, SMS e e-mail transacional.
   - Planejar cobrança/pagamentos, observabilidade, monitoramento de erros, analytics, backups e rate limiting distribuído.
   - Não ativar serviços pagos ou enviar mensagens reais sem confirmar escopo, contas e ambiente com o responsável.

### Regras de continuidade

- Leia primeiro `docs/superpowers/specs/2026-09-09-operacao-clinica-real-design.md` e `docs/superpowers/plans/2026-09-09-operacao-clinica-real.md`.
- Inspecione `git status`, rode os testes existentes e preserve todas as mudanças funcionais.
- Não crie pacientes, agendamentos, notificações, faturamento ou métricas fictícias.
- Mantenha isolamento entre clínicas, RBAC, auditoria e tratamento de erros em qualquer novo endpoint.
- Em UI, sempre implemente loading, erro, vazio e estado não configurado; respeite teclado, leitor de tela e movimento reduzido.
- Antes de declarar conclusão, rode testes, builds, auditoria de dependências e smoke test. Faça commits pequenos e publique em `main` apenas com tudo verde.
- Ao finalizar, reporte objetivamente: arquivos alterados, testes executados, commit, URL de produção e pendências reais.
